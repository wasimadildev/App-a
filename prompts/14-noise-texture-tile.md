# IMAGE 14 — NOISE TEXTURE OVERLAY (512×512, 1:1)
## CSS background texture tile (used at ~3-5% opacity)

---

### Midjourney Prompt

```
pure seamless film grain noise texture tile, entire image is just ultra-fine monochromatic grain on near-black neutral background, no colors no gradients no shapes no objects no lighting effects, looks exactly like analog film grain from high-ISO camera sensor, grain is very fine and uniform across entire image, tiles perfectly seamlessly in all directions, 512x512 pixels, greyscale --ar 1:1 --v 6.1 --s 0 --style raw --no colors,gradients,shapes,objects,pattern repeat,banding
```

### DALL-E 3 Prompt

```
Pure seamless film grain noise texture tile. The entire image is just ultra-fine monochromatic grain on a near-black neutral background. No colors, no gradients, no shapes, no objects, no lighting effects. Looks exactly like analog film grain or photographic noise from a high-ISO camera sensor. The grain is very fine and uniform across the entire image. Must tile perfectly seamlessly in all directions. 512x512 pixels, greyscale.
```

### Stable Diffusion Prompt

```
Positive: pure seamless film grain noise texture, ultra-fine monochromatic grain on near-black background, analog film grain look, high-ISO photographic noise, very fine and uniform grain, seamless tile, 512x512, greyscale
Negative: colors, gradients, shapes, objects, visible pattern repeat, coarse grain, banding, noise clusters, text, white areas
Steps: 20, CFG: 3, Sampler: Euler a
```

---

**Note:** This is the hardest image to generate with AI. Consider generating in Photoshop or using a dedicated noise generator instead:

1. **Photoshop:** Filter → Noise → Add Noise (Monochromatic, Gaussian, 5-10%)
2. **Online:** https://www.noisetexturegenerator.com/
3. **CSS-only alternative:** Use CSS `background-image` with SVG noise filter or `mix-blend-mode` — no image needed.

---

**Aspect Ratio:** 1:1 (512×512)
**Type:** Seamless tileable texture
**Color:** Greyscale, near-black
**Usage:** CSS background at 3-5% opacity
